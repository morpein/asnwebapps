apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: lb-ingress
  namespace: default
spec:
  rules:
# - host: host.domain.com 
    - http:
        paths:
        - path: /*
          backend:
            serviceName: wwwserver 
            servicePort: 80
